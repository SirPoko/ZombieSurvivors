import wollok.game.*
import src.entities.arya.*
program main {
  game.width(50)
  game.height(25)
  game.cellSize(20)
  game.title("Monster Survivors")
  game.boardGround("GrassyPath.jpg")
  game.start()
  game.addVisual(arya)
}